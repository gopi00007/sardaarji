# OS / editor cruft
.DS_Store
Thumbs.db
*.log

# Dependency folders (none needed for this static site, but just in case)
node_modules/

# Local environment secrets (never commit real secret keys here)
.env
.env.local

# Note: js/supabase-config.js contains only the PUBLIC anon key, which is safe
# to commit. Never put the service_role key or database password in the repo.
